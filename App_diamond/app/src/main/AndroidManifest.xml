<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiamonData"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.TeamsActivity" />
        <activity android:name=".activity.CreateTeamActivity" />
        <activity android:name=".activity.TeamDetailActivity" />
        <activity android:name=".activity.MyProfileActivity" />
        <activity android:name=".activity.SeasonsActivity" />
        <activity android:name=".activity.GamesActivity" />
        <activity android:name=".activity.GameBoxScoreActivity" />

    </application>

</manifest>